FROM node:lts-alpine

ARG ARG_MAIL_DOMAIN
ARG ARG_MAIL_API

ENV MAIL_DOMAIN=$ARG_MAIL_DOMAIN
ENV MAIL_API=$ARG_MAIL_API

WORKDIR /app

COPY package*.json ./

RUN yarn

COPY . .

EXPOSE 3000

CMD ["yarn", "build"]
